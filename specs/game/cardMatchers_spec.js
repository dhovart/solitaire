/* eslint no-undef: "error" */
/* global describe it expect */

import cardsMatchers from '../../src/game/cardsMatchers';

describe('cardMatchers', () => {
  it('matches a card of lesser value and of opposite color with the last card of a tableau', () => {
  });
});

